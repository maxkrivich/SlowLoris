language: python

cache: pip

python:
 - "3.8"

install:
  - pip install poetry
  - poetry install -v

# before_script:
#   - poetry run flake8 ./pyslowloris

script:
  - poetry run pytest

branches:
  only:
    - master
    - develop

notifications:
  email:
    recipients:
      -  maxkrivich@gmail.com
    on_success: never
    on_failure: always